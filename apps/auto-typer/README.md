# Auto Typer

A utility that watches a keyboard for typed patterns and auto-completes them
into longer strings.

## Usage

```bash
./auto-typer [keyboard_device]
./auto-typer -h | --help
```

### Arguments
- `keyboard_device`: The USB keyboard device query (defaults to `USB Keyboard`)
- `-h | --help`: Print help
